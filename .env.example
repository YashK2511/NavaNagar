# Local backend endpoint used by the browser. Keep API secrets out of VITE_ variables.
VITE_PLANNING_API_URL=http://localhost:8787
CORS_ORIGIN=http://localhost:5173

# Optional live AI scenario generation. This is read only by the local server.
OPENAI_API_KEY=
AI_MODEL=gpt-5.4-mini
